{
  "buildCommand": "cd frontend && npm install && npm run build && mv dist ../dist_output",
  "outputDirectory": "dist_output",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.py"
    }
  ]
}
